_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
vendor/
.DS_Store

# Working material, not part of the published site: outlines for posts that may never be written,
# and the three posts withdrawn before launch. They stay on disk and out of the public repository.
_notes/

# Authoring tooling. The house rules are for writing the blog, not for building it.
.claude/
